def palindrome(x):
    
    X = x
    inverse=0
    while x!=0:
        inverse=(inverse * 10) + (x % 10)
        x = x // 10

    if X == inverse:
        print("true")
    else:
        print ("false")
        
